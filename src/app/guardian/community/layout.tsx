export default function GuardianCommunityLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  // Parent `src/app/guardian/layout.tsx` supplies the DashboardLayout.
  return <>{children}</>;
}
